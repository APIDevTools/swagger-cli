{
  "name": "swagger-cli",
  "version": "1.0.0-beta.1",
  "description": "Command-line tool to parse, validate, and serve Swagger-based REST APIs",
  "keywords": [
    "swagger",
    "rest",
    "api",
    "yaml",
    "parse",
    "parser",
    "validate",
    "validator",
    "validation",
    "serve",
    "host"
  ],
  "contributors": [
    {
      "name": "James Messinger",
      "url": "http://jamesmessinger.com"
    },
    {
      "name": "Randall Krauskopf",
      "url": "https://github.com/rkrauskopf"
    }
  ],
  "license": "MIT",
  "homepage": "https://github.com/BigstickCarpet/swagger-cli",
  "main": "lib/index.js",
  "bin": {
    "swagger": "lib/swagger-cli.js"
  },
  "scripts": {
    "build": "jshint . --verbose && jscs . --verbose",
    "test": "istanbul cover _mocha -- --recursive tests",
    "mocha": "mocha --recursive tests",
    "update-deps": "npm-check-updastes -u && npm update",
    "release": "npm run update-deps && npm run build && npm test && bump --prompt --tag --push --all && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BigstickCarpet/swagger-cli.git"
  },
  "devDependencies": {
    "chai": "^2.3.0",
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.14",
    "jscs": "^1.13.1",
    "jshint": "^2.8.0",
    "mocha": "^2.2.5",
    "npm-check-updates": "^1.5.1",
    "sinon": "^1.14.1",
    "version-bump-prompt": "^1.3.1"
  },
  "dependencies": {
    "commander": "^2.8.1",
    "lodash": "^3.9.3",
    "swagger-parser": "^2.4.1",
    "swagger-server": "0.0.8"
  }
}
